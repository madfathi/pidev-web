<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commande</title>
    <style>
        /* Style des champs de texte */
        input[type="text"],
        input[type="email"],
        input[type="date"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px; /* Ajout de bord arrondi */
            box-sizing: border-box;
            font-size: 16px;
            margin-bottom: 10px;
        }
        
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa; /* Couleur de fond */
            color: #333; /* Couleur du texte */
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff; /* Couleur de fond du contrat */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Ombre */
            border-radius: 8px;
            border: 1px solid #ddd; /* Bordure */
        }
        header, footer {
            text-align: center;
            padding: 10px 0;
            background-color: #007bff; /* Couleur de fond de l'en-tête et du pied de page */
            color: #fff; /* Couleur du texte de l'en-tête et du pied de page */
            border-radius: 8px 8px 0 0;
        }
        header img {
            max-width: 150px;
            height: auto;
        }
        h1 {
            text-align: center;
            font-size: 28px;
            margin-top: 20px;
            margin-bottom: 20px;
            color: #007bff; /* Couleur du titre */
        }
        .location-date {
            text-align: right;
            margin-bottom: 20px;
            font-size: 16px;
            color: #333; /* Couleur du texte */
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            font-size: 16px;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        td {
            vertical-align: top;
        }
        .contract-info {
            margin-bottom: 30px;
        }
        .signature {
            margin-top: 50px;
            text-align: center;
        }
        .signature p {
            margin-top: 20px;
            font-size: 18px;
        }
        footer p {
            font-size: 20px;
        }

        /* Media Query pour les petits écrans */
        @media only screen and (max-width: 600px) {
            .location-date {
                text-align: left; /* Pour les petits écrans, alignez à gauche */
            }
        }
    </style>
</head>
<body>
    <header>
  
        <h2>PowerFit</h2>
        <p>123 Nour Jaafer, Ariana , Tunisia | Téléphone: +216 345 67890 | mail : PowerFit@hormail.com</p>
    </header>

    <div class="container">
        <h1>Seance</h1>

        <!-- Nouvelle disposition pour la date et le lieu -->
        <div class="location-date">
            <p>Date: {{ "now"|date("d/m/Y") }}</p>
            <p>Lieu: Elghazzela</p>
        </div>

        <!-- Reste du contenu -->
        <div class="contract-info">
            <p><strong>Détails du Seance:</strong></p>
            <table>
                <tbody>
                    <tr>
                        <th>Type Seance</th>
                        <td>{{ client.nom }}</td>
                    </tr>
                    <tr>
                        <th>Duree Seance</th>
                        <td>{{ client.prenom }}</td>
                    </tr>
                    <tr>
                        <th>NB Maximal</th>
                        <td>{{client.age }}</td>
                    </tr>
                    
                    
                    <tr>
                        <th>Categorie</th>
                        <td>{{ client.poids }}</td>
                    </tr>
                    <tr>
                        <th>Date Fin</th>
                        <td>{{ client.hauteur }}</td>
                    </tr>
                  
                </tbody>
            </table>
        </div>
    </div>

    <footer>
        <p>&copy; {{ "now"|date("Y") }} PowerFit. Tous droits réservés 2024.</p>
    </footer>
</body>
</html>