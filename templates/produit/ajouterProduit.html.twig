<!DOCTYPE html>
<html lang="en">
  <head>
     <!-- Required meta tags -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>Dashboard - SB Admin</title>
        <link href="https://cdn.jsdelivr.net/npm/simple-datatables@7.1.2/dist/style.min.css" rel="stylesheet" />
        <link rel="stylesheet" href="/back/assets/css/styles.css">
        <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
  </head>
  <body>
    <div class="container-scroller">
      <!-- partial:partials/_sidebar.html -->
      <nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
            <!-- Navbar Brand-->
            <a class="navbar-brand ps-3" href="{{ path('app_produit_ajouter') }}"> Dashboard</a>
            <!-- Sidebar Toggle-->
            <button class="btn btn-link btn-sm order-1 order-lg-0 me-4 me-lg-0" id="sidebarToggle" href="#!"><i class="fas fa-bars"></i></button>
           {# <form action="{{ path('app_produit_search') }}" method="get" class="nav-link mt-2 mt-md-0 d-none d-lg-flex search">
               <input type="text" name="search" id="search" class="form-control" placeholder="Search products">
             <button type="submit" class="btn btn-primary">Search</button>
            </form>
                    <div id="search-results"></div> #}

            </li>
            <script>
$(document).ready(function() {
    $('#search-form').submit(function(e) {
        e.preventDefault(); // Empêche le formulaire d'être soumis normalement

        // Récupérer la valeur du champ de saisie
        var searchTerm = $('#search').val();

        $.ajax({
            type: 'GET',
            url: '{{ path('app_produit_search') }}', // Endpoint de recherche AJAX
            data: {
                keyword: searchTerm
            },
            success: function(response) {
                // Mettre à jour l'interface utilisateur avec les résultats de la recherche
                $('#product-table-body').html(response);
            }
        });
    });
});
</script>


    </ul>
            
          

          </div>
        </nav>
 
        <!-- partial -->
        <div class="main-panel">
          <div class="content-wrapper">
                             {% for type, messages in app.flashes() %}
    {% for message in messages %}
        {% if type == 'success' %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endif %}
    {% endfor %}
{% endfor %}
          <div class="row">
              <div class="col-md-6 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">Ajouter Produit</h4>
                    <p class="card-description"></p>
                   {% block body %}
                   
                  {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
                   <div class="form-group" >
                   {{ form_label(form.nomproduit, 'Nom de produit', {'attr': {'class': 'form-control-label'}}) }}
                   {{ form_widget(form.nomproduit, {'attr': {'class': 'form-control', 'placeholder': 'Nom de produit'}}) }}
                   {{ form_errors(form.nomproduit) }}

                   </div>
                   <div class="form-group" >
                   {{ form_label(form.categorie, 'categorie', {'attr': {'class': 'form-control-label'}}) }}
                   {{ form_widget(form.categorie, {'attr': {'class': 'form-control', 'placeholder': 'categorie'}}) }}
                   </div>
                   <div class="form-group" >
                   {{ form_label(form.prix, 'Prix', {'attr': {'class': 'form-control-label'}}) }}
                   {{ form_widget(form.prix, {'attr': {'class': 'form-control', 'placeholder': 'Prix'}}) }}
                   {{ form_errors(form.prix) }}

                   </div>
                   <div class="form-group" >
                   {{ form_label(form.quantite, 'Quantite', {'attr': {'class': 'form-control-label'}}) }}
                   {{ form_widget(form.quantite, {'attr': {'class': 'form-control', 'placeholder': 'Quantite'}}) }}
                   {{ form_errors(form.quantite) }}
                   </div>
                   <div class="form-group">
                   {{ form_label(form.imageproduit, 'Image', {'attr': {'class': 'form-control-label'}}) }}
                   {{ form_widget(form.imageproduit, {'attr': {'class': 'form-control'}}) }}
                   {{ form_errors(form.imageproduit) }}

                   </div>
                   <button type="submit" class="btn btn-primary">Ajouter</button>
                   {{ form_end(form) }}
                   {% endblock %}
                  </div>
                </div>
              </div>
             <div class="col-lg-6 grid-margin stretch-card">
             
             <div class="card">

        <div class="card-body">
            <h4 class="card-title">Nombre de produits</h4>
           <div class="chart-container" style="width:350px; height:350px;">
    <canvas id="pieChart"></canvas>

</div>
    </div>
</div>
</div>

                                    <div class="row ">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title text-center">Liste des produits</h4>
                    <div class="table-responsive">
                    <div class="row">
                    
    <div class="col-lg-2 text-center">
    
         <div class="form-group">
            <select class="btn btn-primary" id="sort-options">
                 <option  class="btn btn-primary" value="">Trier par</option>
                <option value="{{ path('app_prix_asc')}}" class="btn btn-primary">Prix croissant</option>
                <option value="{{ path('app_prix_desc')}}" class="btn btn-primary">Prix décroissant</option>
                <option value="{{ path('app_nomproduit_asc')}}" class="btn btn-primary">Nom croissant</option>
                 <option value="{{ path('app_nomproduit_desc')}}" class="btn btn-primary">Nom décroissant</option>


            </select>
        
 <script>
    document.getElementById('sort-options').addEventListener('change', function() {
        var selectedValue = this.value; // Récupérer la valeur de l'option sélectionnée

        // Rediriger l'utilisateur vers l'URL de tri sélectionnée
        if (selectedValue !== '') {
            window.location.href = selectedValue;
        }
    });
</script>
        </div>
    </div>
    <div class="form-group">
    <a class="btn btn-primary" href="{{ path('app_produit_pdf') }}" >PDF</a>
    </div>
</div>
                      <table class="table">
                        <thead>
                          <tr>
                            <th>
                              <div class="form-check form-check-muted m-0">
                                <label class="form-check-label">
                                  <input type="checkbox" class="form-check-input">
                                </label>
                              </div>
                            </th>
                            <th> Nom produit </th>
                            <th> Catégorie </th>
                            <th> Quantité </th>
                            <th> Prix (Dt) </th>
                            <th> Image Produit </th>
                            <th> Actions   </th>
                          </tr>
                        </thead>
       <tbody id="product-table-body">
    {% for produit in produits %}
  <tr>
    <td>
      <div class="form-check form-check-muted m-0">
        <label class="form-check-label">
          <input type="checkbox" class="form-check-input">
        </label>
      </div>
    </td>
    <td>{{ produit.nomproduit }}</td>
    <td>{{ produit.categorie.nomcategorie }}</td>
    <td>{{ produit.quantite }}</td>
    <td>{{ produit.prix |number_format(3, '.', ' ') }}</td>
    
    <td>
      {% if produit.imageproduit %}
        <img src="{{ asset('images/' ~ produit.imageproduit) }}" alt="Image" style="width: 200px; height: auto;">
      {% else %}
        Aucune image
      {% endif %}
    </td>
    <td>
        <form id="deleteForm{{ produit.idproduit }}" action="{{ path('app_produit_supprimer', {'id': produit.idproduit}) }}" method="post" style="display: inline;">
        <button type="button" class="btn btn-danger delete-btn" data-id="{{ produit.idproduit }}">Delete</button>
    </form>
    <form action="{{ path('app_produit_edit', {'id': produit.idproduit}) }}" style="display: inline;"> 
     <button type="submit" class="btn btn-primary">Edit</button>
      </form>
      </td>
      </tr>
         {% endfor %}
      </tbody>
                      </table>
                  <div class="pagination-wrapper">
    {% set pagination_html %}
        {{ knp_pagination_render(pagination) }}    
    {% endset %}
    {% set replaced_html = pagination_html|replace({'<a ': '<a class="btn btn-danger" '}) %}
    {% set replaced_html = replaced_html|replace({'<span class="current">': '<a class="btn btn-danger active" disabled>'}) %}
    {% set replaced_html = replaced_html|replace({'</span>': '</a>'}) %}
    {{ replaced_html|raw }}
</div>


                    </div>
                  </div>
                </div>
              </div>
            </div>


            <script>
document.addEventListener("DOMContentLoaded", function() {
    const deleteButtons = document.querySelectorAll(".delete-btn");

    deleteButtons.forEach(button => {
        button.addEventListener("click", function() {
            const productId = this.getAttribute("data-id");
            const confirmation = confirm("Are you sure you want to delete this product?");

            if (confirmation) {
                const form = document.querySelector("#deleteForm" + productId);
                if (form) {
                    form.submit();
                }
            }
        });
    });
});
</script>
<style>
.pagination-wrapper {
    text-align: center; /* Pour centrer les boutons de pagination */
        margin-top: 20px; /* Ajoute une marge en haut pour plus d'espace */

}

.pagination-wrapper .pagination  {
    display: inline-block; /* Pour afficher les boutons en ligne */
}

.pagination-wrapper .pagination  {
    padding: 10px 10px; /* Pour ajouter de l'espace autour des boutons */
    margin: 0 10px; /* Pour ajouter un petit espace entre les boutons */
}

</style>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    const ctx = document.getElementById('pieChart').getContext('2d');
    const data = {
            labels: {{ categories|map(category => category[0].nomCategorie)|json_encode|raw }},
      datasets: [{
        label: 'nombre de produits :',
        data: {{ categories|map(category => category.count)|json_encode|raw }},
        backgroundColor: [
          '#4e73df',
          '#1cc88a',
          '#36b9cc',
          '#f6c23e',
          '#e74a3b',
          '#9b59b6',
          '#fbc02d',
          '#7266ba',
          '#4882c2',
          '#6d5e63'
        ],
        borderColor: [
          '#4e73df',
          '#1cc88a',
          '#36b9cc',
          '#f6c23e',
          '#e74a3b',
          '#9b59b6',
          '#fbc02d',
          '#7266ba',
          '#4882c2',
          '#6d5e63'
        ],
        borderWidth: 1
      }]
    };

    const config = {
      type: 'pie',
      data: data,
      /*options: {
        scales: {
          y: {
            beginAtZero: true
          }
        }
      }*/
    };

    new Chart(ctx, config);
  </script>
          <!-- content-wrapper ends -->
          <!-- partial:partials/_footer.html -->
          <footer class="footer">
            <div class="d-sm-flex justify-content-center justify-content-sm-between">
              <span class="text-muted d-block text-center text-sm-left d-sm-inline-block">Copyright © bootstrapdash.com 2020</span>
              <span class="float-none float-sm-right d-block mt-1 mt-sm-0 text-center"> Free <a href="https://www.bootstrapdash.com/bootstrap-admin-template/" target="_blank">Bootstrap admin templates</a> from Bootstrapdash.com</span>
            </div>
          </footer>
          {# <!-- partial -->
        </div>
        <!-- main-panel ends -->
      </div>
      <!-- page-body-wrapper ends -->
    </div>
    <!-- container-scroller -->
    <!-- plugins:js -->
    <script src="back/assets/vendors/js/vendor.bundle.base.js"></script>
    <!-- endinject -->
    <!-- Plugin js for this page -->
    <script src="back/assets/vendors/chart.js/Chart.min.js"></script>
    <script src="back/assets/vendors/progressbar.js/progressbar.min.js"></script>
    <script src="back/assets/vendors/jvectormap/jquery-jvectormap.min.js"></script>
    <script src="back/assets/vendors/jvectormap/jquery-jvectormap-world-mill-en.js"></script>
    <script src="back/assets/vendors/owl-carousel-2/owl.carousel.min.js"></script>
    <!-- End plugin js for this page -->
    <!-- inject:js -->
    <script src="back/assets/js/off-canvas.js"></script>
    <script src="back/assets/js/hoverable-collapse.js"></script>
    <script src="back/assets/js/misc.js"></script>
    <script src="back/assets/js/settings.js"></script>
    <script src="back/assets/js/todolist.js"></script>
    <!-- endinject -->
    <!-- Custom js for this page -->
    <script src="back/assets/js/dashboard.js"></script>
    <!-- End custom js for this page --> #}
  </body>
</html>