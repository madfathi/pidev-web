{% extends 'base.html.twig' %}

{% block contentsq %}
    <a href="{{ path('app_user_add') }}" class="btn btn-primary">Ajouter User</a>
    <button id="sortByNameBtn" class="btn btn-primary">Trier par Nom</button> <!-- Bouton de tri par nom -->
    <table class="table">
        <thead>
            <tr>
             
                <th>id</th>
                <th>urlImage</th>
                <th>cin</th>
                <th>nom</th>
                <th>prenom</th>
                <th>username</th>
                <th>plainPassword</th>
                <th>email</th>
                <th>role</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                                        <td>{{ user.urlImage }}</td>

                    <td>{{ user.nom }}</td>
                    <td>{{ user.prenom }}</td>
                                        <td>{{ user.username }}</td>

                    <td>{{ user.plainPassword }}</td>
                    <td>{{ user.role }}</td>
                    <td>
                        <a href="{{ path('app_user_edit', {'id': user.id }) }}" class="btn btn-success">
                            <i class="fas fa-edit"></i> Edit
                        </a>
                        <a href="{{ path('app_user_delete', { 'id': user.id }) }}" class="btn btn-danger" style="display: inline-block;">
                            <i class="fas fa-trash-alt"></i> Delete
                        </a>
                       *
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7">no records found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
