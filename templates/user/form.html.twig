{% extends 'base.html.twig' %}
 {% block bodyse %}
 <div id="main">
        <!-- Post -->
        <section class="post">
            <header class="major">
                <span>   <h1 style="width: 200%;">Create new User</h1>
</span>
            </header>

            <div class="section-content">
              <div class="container">
<div class="form-container">
                {{ form_start(form , {'attr': {'class':'form-horizontal','novalidate':'novalidate'}} ) }}
                <div class="col-md-12">
               
                  <fieldset>
    {{ form_label(form.nom, 'Nom', {'label_attr': {'class': 'custom-label'}}) }}
    {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
    {% if form_errors(form.nom) %}
        <div class="error-message">
            {{ form_errors(form.nom) }}
        </div>
    {% endif %}
</fieldset>
                </div>

                <div class="col-md-12">
                <fieldset>
    {{ form_label(form.prenom, 'Prenom', {'label_attr': {'class': 'custom-label'}}) }}
    {{ form_widget(form.prenom, {'attr': {'class': 'form-control'}}) }}
    {% if form_errors(form.prenom) %}
        <div class="error-message">
            {{ form_errors(form.prenom) }}
        </div>
    {% endif %}
    <fieldset>
    {{ form_label(form.username, 'Username', {'label_attr': {'class': 'custom-label'}}) }}
    {{ form_widget(form.username, {'attr': {'class': 'form-control'}}) }}
    {% if form_errors(form.username) %}
        <div class="error-message">
            {{ form_errors(form.username) }}
        </div>
    {% endif %}



</fieldset>
                </div>

                <div class="col-md-12">
   <style>
    .hidden-row {
        display: none;
    }
</style>



<div class="col-md-12">
  <fieldset>
    {{ form_label(form.email, 'Email', {'label_attr': {'class': 'custom-label'}}) }}
    {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
    {% if form_errors(form.email) %}
        <div class="error-message">
            {{ form_errors(form.email) }}
        </div>
    {% endif %}
</fieldset>
</div>
             <fieldset>
    {{ form_label(form.role, 'Sélectionner un Role :', {'label_attr': {'class': 'custom-label'}}) }}
    {{ form_widget(form.role, {'attr': {'class': 'form-control'}}) }}
    {% if form_errors(form.role) %}
        <div class="error-message">
            {{ form_errors(form.role) }}
        </div>
    {% endif %}
</fieldset>
                </div>
                <div class="col-md-12">
                   <fieldset>
    <button type="submit" class="btn btn-success" style="margin-top: 10px; margin-bottom: 20px;"> Reserver ! </button>
</fieldset>
                </div>
  
            

                {{ form_end(form) }}
                
</div>
            </div>
             </div>
        </section>
 <script>
 
  function previewImage(event) {
    var input = event.target;
    var preview = document.getElementById('image_preview');

    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            preview.src = e.target.result;
            preview.style.display = 'block'; // Afficher l'aperçu de l'image
        }

        reader.readAsDataURL(input.files[0]); // Convertir l'image en URL de données
    }
}
       

    </script>


    </div>

  {% endblock %}


  





 