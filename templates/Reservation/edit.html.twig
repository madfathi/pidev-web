{% extends 'base.html.twig' %}
 {% block bodyse %}
 <div id="main">
        <!-- Post -->
        <section class="post">
            <header class="major">
                <span>   <h1 style="width: 200%;">Modify Reservation</h1>
</span>
            </header>

            <div class="section-content">
              <div class="container">
<div class="form-container">
                {{ form_start(form , {'attr': {'class':'form-horizontal','novalidate':'novalidate'}} ) }}
                <div class="col-md-12">
               
                    <fieldset>
                     {{ form_row(form.typeReservation, {
    'attr': {
        'placeholder': 'Type_Reservation',
        'class': 'custom-placeholder'
    },
    'label_attr': {
        'class': 'custom-label'
    }
}) }}
                    </fieldset>
                </div>

                <div class="col-md-12">
                    <fieldset>
                     {{ form_row(form.username, {
    'attr': {
        'placeholder': 'Duree_username',
        'class': 'custom-placeholder'
    },
    'label_attr': {
        'class': 'custom-label'
    }
}) }}
                    </fieldset>
                </div>

                <div class="col-md-12">
                    <fieldset>
                       {{ form_row(form.email, {
    'attr': {
        'placeholder': 'email',
        'class': 'custom-placeholder'
    },
    'label_attr': {
        'class': 'custom-label'
    }
}) }}
                    </fieldset>
                </div>
                  <div class="col-md-12">
                <fieldset style="display: none;">
    {{ form_row(form.idSeance, {
        'attr': {
            'placeholder': 'email',
            'class': 'custom-placeholder'
        },
        'label_attr': {
            'class': 'custom-label'
        }
    }) }}
</fieldset>
                </div>

                <div class="col-md-12">
   <style>
    .hidden-row {
        display: none;
    }
</style>



<div class="col-md-12">
    <fieldset>
      
      {{ form_row(form.phone, {'attr':{'placeholder': 'Confirm_password', 'onchange': 'previewImage(event)'}}) }}
    </fieldset>
</div>
             
    <button type="submit" class="btn btn-success" style="margin-top: 10px; margin-bottom: 20px;"> Reserver ! </button>
</fieldset>
                </div>
  
            

                {{ form_end(form) }}
                
</div>
            </div>
             </div>
        </section>
 <script>
 
  function previewImage(event) {
    var input = event.target;
    var preview = document.getElementById('image_preview');

    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            preview.src = e.target.result;
            preview.style.display = 'block'; // Afficher l'aperçu de l'image
        }

        reader.readAsDataURL(input.files[0]); // Convertir l'image en URL de données
    }
}
       

    </script>


    </div>

  {% endblock %}





 